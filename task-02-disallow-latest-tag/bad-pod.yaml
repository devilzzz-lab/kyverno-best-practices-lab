apiVersion: v1
kind: Pod
metadata:
  name: bad-pod-latest
spec:
  containers:
    - name: nginx
      image: nginx:latest
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
        limits:
          cpu: 200m
          memory: 256Mi
